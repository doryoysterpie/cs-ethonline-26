{
  "name": "@cas/drafting",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic Cyberattack Sunday drafting: reviewed incidents to an editable draft with a provenance sidecar. Pure, model-free. Sprint 5, decision D25.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@cas/contracts": "workspace:*"
  }
}
