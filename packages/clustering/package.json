{
  "name": "@cas/clustering",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic incident clustering: exact URL duplicates, syndication and conservative incident grouping, driven by a versioned executable contract. Pure: no database, network, environment, model, clock or randomness. Sprint 4, decision D22.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@cas/contracts": "workspace:*"
  }
}
