{
  "name": "@cas/classification",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic high-recall classification of imported sources into include, exclude or review, and the post-hoc calibration evaluator. Pure: no database, network, environment, model, clock or randomness. Sprint 3, decision D21.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cas/contracts": "workspace:*",
    "@cas/taxonomy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
