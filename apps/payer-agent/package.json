{
  "name": "@cas/payer-agent",
  "version": "0.0.0",
  "private": true,
  "description": "x402 payer agent application, Sprint 7 conditional on the Graph gate. Placeholder after Sprint 0.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
