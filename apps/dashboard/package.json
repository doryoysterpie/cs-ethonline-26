{
  "name": "@cas/dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "Editorial dashboard application. Placeholder after Sprint 0.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
