# CAS-008: Supporter tier and donations

**Normative contract:** `docs/POST-EVENT-PLAN.md`, section 5, CAS-008.
**Status:** Queued. **Primary engineer:** Claude Code. **Independent reviewer:** Codex.
**Merge authority:** the owner, by hand.
**Depends on:** CAS-007. **Blocks:** nothing.
**Branch:** none yet; named at S0 as `cas-008/<short-name>` from `main`.

## Objective and boundary

Reading stays free. A paid supporter tier and one-off crypto donations exist, and neither gates
anything a reader needs. Payment is a support mechanism, not access control.

In scope: the fiat provider decision, made without vendor allegiance at S0, with provider-hosted
checkout so no card data ever touches the service; webhook handling with signature
verification, idempotency and append-only supporter status; a donation page showing the owner's
crypto addresses, pinned in a policy file the way the workbook digest is; audit events; the
tests at step 9.

Out of scope: any on-chain contract; the hackathon's x402 payer agent, whose disposition is
CAS-006's; cards.

## Unit steps, in order

- [ ] 1. `[HUMAN]` Decide the fiat provider. Candidates are named at S0 with their fees, the
      countries they pay out to, and whether they host the checkout page; the owner chooses and
      records the ground.
- [ ] 2. `[HUMAN]` Decide what the supporter tier grants, if anything, beyond support. Record
      either way; "nothing beyond a thank-you" is a valid answer.
- [ ] 3. `[HUMAN]` Decide which chains accept donations and supply each address from a wallet
      the owner controls. The address is verified by a small test transfer before it is pinned.
- [ ] 4. `[CLAUDE]` S0 design note: the supporter record (account, provider subscription
      identifier, status, event history) as append-only rows; the webhook path; the
      `data/policy/donation-addresses.json` file with each address, chain and a verified-on
      date; the fixed refusal codes.
- [ ] 5. `[CLAUDE]` S1 stack check: the provider's server-side library, if any, named with its
      exact version, reason and rejected alternative (raw signed-webhook verification with
      Node's crypto is the alternative to weigh).
- [ ] 6. `[CLAUDE]` S2 migration at the next free number, forward-only, checksum recorded.
- [ ] 7. `[CLAUDE]` S3 reference implementation: webhook signature verification and the
      supporter transitions against recorded provider events; exit 0 from a clean clone with no
      network.
- [ ] 8. `[CLAUDE]` S5 interface: a "support" page that hands off to the provider-hosted
      checkout; the return page; the donation page rendering the pinned addresses with a QR
      code generated in-process, never fetched.
- [ ] 9. `[CLAUDE]` S4 tests: a replayed webhook changes nothing; a forged webhook is refused
      and logged as a fixed code; an event out of order is handled; a supporter and a free reader
      receive the same content unless step 2 decided a grant; the donation page shows only the
      pinned addresses; a change to the policy file without a commit is detected as drift.
- [ ] 10. `[HUMAN]` with `[CLAUDE]` One real subscription in the provider's test mode, then one
      real donation of a small amount to each pinned address; record the audit events by code.
- [ ] 11. `[CLAUDE]` S7 documentation, including `docs/SECURITY.md` for the webhook boundary and
      `docs/THREAT_MODEL.md` for the new entry point; S8 verification in a clean checkout; CI
      green; diff narrated.
- [ ] 12. `[CODEX]` Independent review. `[HUMAN]` Merge after findings are closed.

## Standard steps

Verbatim from the plan, section 4.1. None may be skipped; a step that does not apply is marked
`N/A` with a reason.

- [ ] S0 Scope and decisions
- [ ] S1 Stack check
- [ ] S2 Data contract
- [ ] S3 Reference implementation
- [ ] S4 Tests
- [ ] S5 Interface wired
- [ ] S6 Security baseline
- [ ] S7 Documentation
- [ ] S8 Verification
- [ ] S9 Review and acceptance

## Acceptance

- A reader starts a subscription through provider-hosted checkout and returns as a supporter
  after the webhook.
- A replayed webhook changes nothing; a forged webhook is refused and logged as a fixed code.
- The donation page shows the pinned addresses and nothing else; changing an address requires
  a commit.
- A supporter and a free reader see the same content unless the owner decided a grant.

## Evidence record

| Field                                | Current value |
| ------------------------------------ | ------------- |
| Plan version                         | 0.2           |
| Branch and commits                   | none          |
| Fiat provider                        | open          |
| Tier grant                           | open          |
| Chains and addresses                 | open          |
| Migration checksum                   | none          |
| Test counts, from the runner         | not run       |
| First real subscription and donation | not run       |
| Clean-checkout verify                | not run       |
| CI run                               | none          |
| Independent review                   | not requested |
| Audit state                          | not audited   |

## Blockers and restart condition

- Blocker: CAS-007 not Complete.
- Restart condition: none; the unit has not started.

## Walk-through log

| Date | Step | Outcome | Evidence |
| ---- | ---- | ------- | -------- |
