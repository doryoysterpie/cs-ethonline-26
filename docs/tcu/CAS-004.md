# CAS-004: Round-up generation through the rundown template, and the owner editor

**Normative contract:** `docs/POST-EVENT-PLAN.md`, section 5, CAS-004.
**Status:** Queued. **Primary engineer:** Claude Code. **Independent reviewer:** Codex.
**Merge authority:** the owner, by hand.
**Depends on:** CAS-003. **Blocks:** CAS-005.
**Branch:** none yet; named at S0 as `cas-004/<short-name>` from `main`.

## Objective and boundary

From a persisted selection, generate a round-up by filling the rundown template with a model,
for a cyberattack or a crypto selection alike, editable by the owner alone, exported as
copy-ready Markdown.

A round-up is the selected stories reformatted into the template's entry format, cleaned and
deduplicated, in the template's order. It is the article type the owner produces today with
Claude. It is not an original article: the model reformats recorded fields and invents nothing.

In scope: D9 closed for this output type; the rundown template as a versioned, hashed file the
owner can edit; an owner role or account flag carrying `generate:roundup` and `edit:output`; a
server-side generation service that receives only the selected stories' recorded fields and
returns the template's slots with per-entry citations; the deterministic draft kept as revision
0 and the generated round-up stored as revision 1 with model identifier, prompt hash, template
hash and input digest; owner edits as later revisions under the existing optimistic concurrency;
a copy-ready export; golden tests over recorded responses; a fail-closed test with the key
absent.

Out of scope: reading source articles in full; original prose beyond the template's slots;
posting to Substack; images; generation for any other account.

Security necessity: every generated entry cites source rows from the selection, and a citation
outside the selection rejects the whole response. Without it the provenance chain from import to
draft breaks at the last step.

## Unit steps, in order

- [ ] 1. `[HUMAN]` Provide the rundown template's first version as structure: the sections and
      their order, the entry format for one story, how deduplicated stories are presented, and
      which parts are the owner's "extras" that stay manual. Structure only; no article text
      enters the repository. One template serves both cyberattack and crypto round-ups unless
      the owner says otherwise.
- [ ] 2. `[HUMAN]` Resolve D9 for the round-up: the model identifier, deterministic settings,
      structured output, maximum tokens per generation and a monthly spending cap. Consult the
      current API reference at the time of the decision rather than memory. Record with ground.
- [ ] 3. `[HUMAN]` Decide the owner mechanism. Recommendation: a fourth role `owner` in the
      existing role table, holding the editor's capabilities plus `generate:roundup`,
      `generate:article` (reserved for CAS-005) and `edit:output`. Record.
- [ ] 4. `[CLAUDE]` S0 design note and round-up contract: the template file's location, version
      and hash; the slot schema; per-entry citations to source rows; D4's naming rule applied to
      generated entries; the Zod schema; the rule that a citation outside the selection rejects
      the response.
- [ ] 5. `[CLAUDE]` Prompt contract: a versioned, hashed system prompt that instructs
      reformatting only; the input is the recorded fields of the selected stories (title,
      summary text, publisher, URL, category, incident grouping), never notes, never other
      rows, never fetched pages; the output is JSON matching the slot schema.
- [ ] 6. `[CLAUDE]` S1 stack check: the model client dependency named with its exact version
      through the workspace catalogue, its reason, the rejected alternative, the release-age
      control respected; lockfile in the same commit.
- [ ] 7. `[CLAUDE]` S2 data contract: where a generated revision lives, either the existing
      draft-revision record with a revision kind and model fields, or a new table; decided and
      stated; migration at the next free number, forward-only, checksum recorded.
- [ ] 8. `[CLAUDE]` S3 reference implementation: the generation function with an injected
      transport, run against a recorded response fixture, validating the slot schema and the
      citations; exit 0 from a clean clone with no network.
- [ ] 9. `[CLAUDE]` S4 tests: golden recorded responses for a cyberattack and a crypto
      selection; a citation outside the selection is rejected; key absent gives a fixed refusal
      and writes nothing; cap exceeded refuses; a non-owner is refused at the data-access layer
      with a fixed code; a template hash mismatch refuses; the default suite opens no socket.
- [ ] 10. `[CLAUDE]` S5 interface: the generate control on the selection page for the owner
      role; the existing editor over revisions; a copy-ready Markdown export.
- [ ] 11. `[CLAUDE]` S6 audit events: generation requested and completed, with token counts and
      cost estimate, never content.
- [ ] 12. `[HUMAN]` with `[CLAUDE]` First real round-up over one past week's cyberattack
      selection, then one crypto selection; the owner edits and copies; record time taken and
      cost.
- [ ] 13. `[CLAUDE]` S7 documentation: `README.md`, `docs/ARCHITECTURE.md`,
      `docs/DECISIONS.md`, `docs/SECURITY.md` for the new capability and the model boundary,
      the unit's report.
- [ ] 14. `[CLAUDE]` S8 verification in a clean checkout; CI green; diff narrated.
- [ ] 15. `[CODEX]` Independent review. `[HUMAN]` Merge after findings are closed.

## Standard steps

Verbatim from the plan, section 4.1. None may be skipped; a step that does not apply is marked
`N/A` with a reason.

- [ ] S0 Scope and decisions
- [ ] S1 Stack check
- [ ] S2 Data contract
- [ ] S3 Reference implementation
- [ ] S4 Tests
- [ ] S5 Interface wired
- [ ] S6 Security baseline
- [ ] S7 Documentation
- [ ] S8 Verification
- [ ] S9 Review and acceptance

## Acceptance

- The owner selects stories, requests a round-up, and receives an editable round-up in the
  template's structure naming every source it used.
- The same flow works for a crypto selection with the same template.
- A non-owner account sees no control, and a direct request is refused with a fixed code.
- The cost of each generation is recorded as an audit event.
- With the key absent the request fails closed with a fixed message and writes nothing.
- A recorded response whose citation names a row outside the selection is rejected in a test.

## Evidence record

| Field                        | Current value |
| ---------------------------- | ------------- |
| Plan version                 | 0.2           |
| Branch and commits           | none          |
| Rundown template received    | no            |
| D9 for the round-up          | unresolved    |
| Owner mechanism              | open          |
| Model client version         | none          |
| Migration checksum           | none          |
| Test counts, from the runner | not run       |
| First real round-up          | not run       |
| Clean-checkout verify        | not run       |
| CI run                       | none          |
| Independent review           | not requested |
| Audit state                  | not audited   |

## Blockers and restart condition

- Blocker: CAS-003 not Complete.
- Restart condition: none; the unit has not started.

## Walk-through log

| Date | Step | Outcome | Evidence |
| ---- | ---- | ------- | -------- |
