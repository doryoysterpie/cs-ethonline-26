# CAS-002: Categories

**Normative contract:** `docs/POST-EVENT-PLAN.md`, section 5, CAS-002.
**Issue, live status:** [#4](https://github.com/doryoysterpie/cs-ethonline-26/issues/4). Steps are ticked on the issue; this file is the
normative step list.
**Status:** Queued. **Primary engineer:** Claude Code. **Independent reviewer:** Codex.
**Merge authority:** the owner, by hand.
**Depends on:** CAS-001. **Blocks:** CAS-003.
**Branch:** none yet; named at S0 as `cas-002/<short-name>` from `main`.

## Objective and boundary

Every imported story receives a decision per category, beginning with cyberattack and crypto.
Adding a category is a policy change with tests, not a code change across packages.

In scope: a category contract alongside the existing signal policy; a classifier run that emits
one decision per category per row; a migration that stores the category with each result; the
review queue filtered by category; calibration of cyberattack against CS79 and CS86; calibration
of crypto against a labelled set the owner provides, or an explicit record that crypto recall is
unmeasured in v0.

Out of scope: round-up or article generation; any change to clustering's grouping rules; a model call,
unless the owner decides at S0 that classification uses one.

## Unit steps, in order

- [ ] 1. `[HUMAN]` Decide at S0 whether category classification stays deterministic (an
      amendment to D21) or uses a model (which reopens D9's parameters). The plan's
      recommendation, a cost judgment: deterministic per-category rules first, because the
      cyber rules exist and calibrate. Record the decision with its ground.
- [ ] 2. `[HUMAN]` Answer: does a labelled crypto weekly set exist? If yes, name the tab or rows.
      If no, record "crypto recall unmeasured in v0" as a named seam.
- [ ] 3. `[CLAUDE]` S0 design note: the category contract in `@cas/taxonomy` (identifier,
      label, policy identifier and version, hash), with cyberattack bound to the existing
      `classification-signal-policy@1` and crypto to a new policy; the result shape, one row per
      source row per category; how pre-category runs are reported (validated, never
      reinterpreted, per D26).
- [ ] 4. `[CLAUDE]` S2 migration `0012`, forward-only, checksum recorded: the category on
      classification results and on the queue read path; existing rows validated, not
      repaired.
- [ ] 5. `[CLAUDE]` S3 reference implementation: the pure classifier takes a category policy and
      emits a decision; the runtime input guard is unchanged; exit 0 from a clean clone.
- [ ] 6. `[CLAUDE]` S4 tests: per-category decisions; a fixture-only third category proves that
      adding one touches only the policy package and its tests; the calibration evaluator per
      category; failure modes for an unknown category, a policy hash mismatch and a missing
      policy file; PostgreSQL tests for run, report and queue per category.
- [ ] 7. `[CLAUDE]` S5 interface: `classification run`, `report`, `queue` and `calibrate` take a
      category; the root scripts follow.
- [ ] 8. `[CLAUDE]` S5 dashboard read path: the queue page filters by category through the
      existing data-access function; no capability changes.
- [ ] 9. `[CLAUDE]` Calibration on real data: cyberattack against CS79 and CS86 reported as
      counts, as Sprint 3 did; crypto against the owner's set, or the seam recorded.
- [ ] 10. `[CLAUDE]` S7 documentation: `README.md` status, `docs/ARCHITECTURE.md` taxonomy and
      classification rows, `docs/DECISIONS.md`, the unit's report.
- [ ] 11. `[CLAUDE]` S8 verification in a clean checkout; CI green; diff narrated.
- [ ] 12. `[CODEX]` Independent review. `[HUMAN]` Merge after findings are closed.

## Standard steps

Verbatim from the plan, section 4.1. None may be skipped; a step that does not apply is marked
`N/A` with a reason.

- [ ] S0 Scope and decisions
- [ ] S1 Stack check
- [ ] S2 Data contract
- [ ] S3 Reference implementation
- [ ] S4 Tests
- [ ] S5 Interface wired
- [ ] S6 Security baseline
- [ ] S7 Documentation
- [ ] S8 Verification
- [ ] S9 Review and acceptance

## Acceptance

- A classification run on the real ledger writes a decision for every row and every category.
- Retention recall for cyberattack against CS79 and CS86 is reported as counts from the runner.
- A third, fixture-only category added in tests requires no change outside the policy package
  and its tests.
- The queue shows one category at a time.
- Crypto recall is either reported as counts or recorded as unmeasured, in those words.

## Evidence record

| Field                            | Current value |
| -------------------------------- | ------------- |
| Plan version                     | 0.1           |
| Branch and commits               | none          |
| Decision: deterministic or model | open          |
| Crypto labelled set              | unknown       |
| Migration 0012 checksum          | none          |
| Test counts, from the runner     | not run       |
| Calibration counts               | not run       |
| Clean-checkout verify            | not run       |
| CI run                           | none          |
| Independent review               | not requested |
| Audit state                      | not audited   |

## Blockers and restart condition

- Blocker: CAS-001 not Complete.
- Restart condition: none; the unit has not started.

## Walk-through log

| Date | Step | Outcome | Evidence |
| ---- | ---- | ------- | -------- |
