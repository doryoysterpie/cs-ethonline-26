# CAS-007: Reader access

**Normative contract:** `docs/POST-EVENT-PLAN.md`, section 5, CAS-007.
**Issue, live status:** [#9](https://github.com/doryoysterpie/cs-ethonline-26/issues/9). Steps are ticked on the issue; this file is the
normative step list.
**Status:** Queued. **Primary engineer:** Claude Code. **Independent reviewer:** Codex.
**Merge authority:** the owner, by hand.
**Depends on:** CAS-005, by the rule that nothing is built for other accounts before the owner's
weekly flow works. **Blocks:** CAS-008, CAS-009.
**Branch:** none yet; named at S0 as `cas-007/<short-name>` from `main`.

## Objective and boundary

Anyone can request an account on the owner's instance with an email address alone; the owner
approves each request; an approved reader holds the `reader` role and sees public incident
metadata only.

What exists today: passwordless sign-in by one-time code over the Resend email provider;
accounts provisioned by an admin only; an unknown address receives the same generic response
as an unapproved one; roles `judge`, `editor` and `admin`; throttling on sign-in.

In scope: a sign-up request page (address, then the code that proves the requester holds it,
then a pending request record); rate limits per address and per network source; a
pending-approvals page for the admin with approve and deny as audit events; a `reader` role
written out in full; an approval email through the existing provider; reader account expiry; a
migration for sign-up requests; the tests listed at step 9.

Also in scope, from the owner's requirement of 16 September 2026 ("humans yes, AI crawlers
that will hurt me financially no"): rate limits on every unauthenticated route and on reader
reads per account; a `robots.txt` served by the instance that disallows known AI crawlers, a
convention they may ignore, so the limits are the protection; the same limits on any public API
if one is ever exposed.

Out of scope: payments; cards; any change to the owner's capabilities; changes to what editors
see.

Security necessity: sign-up is an unauthenticated entry point that sends email. Without
per-address and per-source limits it is a mail cannon at the owner's cost; without generic
responses it enumerates accounts; without a deny-by-default reader role it leaks editorial
content.

## Unit steps, in order

- [ ] 1. `[HUMAN]` Decide what a reader sees, page by page, within D6's allowlist (public
      incident metadata; no notes, no source text, no drafts). Record.
- [ ] 2. `[HUMAN]` Decide whether the `judge` role is retired into `reader` or kept, together
      with CAS-006's dispositions; decide reader account expiry. Record.
- [ ] 3. `[CLAUDE]` S0 design note: the request record (address hash, requested-at, verified-at,
      decided-at, decision, deciding admin), its composite keys and append-only rule; the exact
      generic responses; the limits per address and per source and their fixed refusal; the
      approval email's fixed text; which existing sign-in code is reused unchanged.
- [ ] 4. `[CLAUDE]` S2 migration at the next free number, forward-only, checksum recorded:
      sign-up requests and the `reader` role, with the schema-security pattern.
- [ ] 5. `[CLAUDE]` S3 reference implementation: request, verify, approve and deny as pure
      transitions over the store interface; exit 0 from a clean clone.
- [ ] 6. `[CLAUDE]` S5 interface: the request page and the verify step, reusing the sign-in
      code path; the admin pending-approvals page with approve and deny behind
      `admin:accounts`; the `reader` role in `roles.ts`, written out in full.
- [ ] 7. `[CLAUDE]` S6: rate limits on the request path keyed on the address and the network
      source the trusted proxy reports (CAS-006 blocker 3 settles which hop); audit events with
      identifiers and fixed codes only; the approval email carries no link that signs in.
- [ ] 8. `[CLAUDE]` Approval and denial emails through the existing provider, fixed text.
- [ ] 8a. `[CLAUDE]` S6 scrape protection: a per-account read limit for readers and a per-source
      limit for every unauthenticated route, each a fixed refusal with a retry-after; a
      `robots.txt` route listing the AI crawler user agents to disallow, the list fixed at S0 and
      versioned; a count-only audit event when a limit trips. Whether the hosting layer offers
      bot management is checked at S0 and recorded, not assumed.
- [ ] 9. `[CLAUDE]` S4 tests: a burst of requests from one source is throttled; a burst for one
      address is throttled; a reader exceeding the read limit is refused with a fixed response
      and the audit event is count-only; `robots.txt` is served with the fixed list; a wrong code five times locks the request; approval by a non-admin
      is refused with a fixed code; a reader requesting an editor or owner page is refused; a
      reader's page responses contain no source text (selected as `NULL` by the database, as
      today); an unknown and a pending address receive byte-identical responses; PostgreSQL
      tests for every transition.
- [ ] 10. `[HUMAN]` with `[CLAUDE]` First real sign-up by the owner from a second address,
      approved from the admin page; record the audit events by code.
- [ ] 11. `[CLAUDE]` S7 documentation: `README.md`, `docs/ARCHITECTURE.md`, `docs/SECURITY.md`,
      `docs/THREAT_MODEL.md` boundary B9 updated from planned to built, `docs/DECISIONS.md`,
      the unit's report.
- [ ] 12. `[CLAUDE]` S8 verification in a clean checkout; CI green; diff narrated.
- [ ] 13. `[CODEX]` Independent review. `[HUMAN]` Merge after findings are closed.

## Standard steps

Verbatim from the plan, section 4.1. None may be skipped; a step that does not apply is marked
`N/A` with a reason.

- [ ] S0 Scope and decisions
- [ ] S1 Stack check
- [ ] S2 Data contract
- [ ] S3 Reference implementation
- [ ] S4 Tests
- [ ] S5 Interface wired
- [ ] S6 Security baseline
- [ ] S7 Documentation
- [ ] S8 Verification
- [ ] S9 Review and acceptance

## Acceptance

- A stranger requests access with only an email address and learns nothing about whether it
  was known.
- The owner sees the request, approves it, and the reader signs in with a code and sees only
  the allowed pages.
- A burst of requests from one source is throttled with a fixed response.
- A reader requesting an editor or owner page is refused with a fixed code.
- A reader's page responses contain no source text.

## Evidence record

| Field                        | Current value |
| ---------------------------- | ------------- |
| Plan version                 | 0.2           |
| Branch and commits           | none          |
| What a reader sees           | open          |
| Judge role disposition       | open          |
| Migration checksum           | none          |
| Test counts, from the runner | not run       |
| First real sign-up           | not run       |
| Clean-checkout verify        | not run       |
| CI run                       | none          |
| Independent review           | not requested |
| Audit state                  | not audited   |

## Blockers and restart condition

- Blocker: CAS-005 not Complete.
- Restart condition: none; the unit has not started.

## Walk-through log

| Date | Step | Outcome | Evidence |
| ---- | ---- | ------- | -------- |
